{
  "name": "layouts-vulpix-pack-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run exemple",
    "exemple": "cd ./exemples/dashboard && npm run dev",
    "version": "cd ./lib && npm run version",
    "publish": "cd ./lib && npm publish",
    "watch": "cd ./lib && npm run preview-watch",
    "install": "cd ./lib && npm install && cd ../exemples/dashboard && npm install"
  }
}
